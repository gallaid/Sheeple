using UnityEngine;
using System.Collections;

public class NPCShape : MonoBehaviour {

    public
	// Use this for initialization
	void Start () {
 //      Texture2D  TextureA[] = new Texture2D[20];
 //       for(int i=0; i<20; i++)
 //           TextureA[i]= TextureA.
	//}


    //void OnTriggerEnter2D(Collider2D player)
    //{
    //    shape = (int)Random.Range(0.0f, 19.99f);
    }
	
	// Update is called once per frame
	void Update () {
        //if (Collision2D.Equals( PlayerCtrl, NPCShape)
        //{
            
        //}
	}

    //bool shapeMatch(int pShape)
    //{
    //    return (pShape == shape);
    //}
}
